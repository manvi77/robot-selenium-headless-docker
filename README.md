# robot-selenium-headless-docker
To build a Docker image based on this Dockerfile, run:  
docker build -t automationsetup .

Run Docker containers based on the custom image:      
docker run --rm automationsetup
